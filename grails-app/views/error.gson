response.status 500
Exception ex = request.getAttribute('exception') as Exception

json {
  error 500
  if (ex) {
    stackTrace (ex.stackTrace) { StackTraceElement ste ->
      "${ste}"
    }
    message "${ex.message}"

  } else {
    message "Unknown Internal server error"
  }
}

