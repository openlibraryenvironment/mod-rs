import groovy.transform.*
import org.olf.rs.Shipment;

@Field Shipment shipment
json g.render(shipment, [expand: ['id',
                                       'shipDate'
                                       ],
                              excludes: ['audit']])
