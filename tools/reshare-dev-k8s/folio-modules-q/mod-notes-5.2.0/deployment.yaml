# -*- mode: yaml; -*-
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "mod-notes-5-2-0"
  labels:
    app: "mod-notes-5-2-0"
    module: "mod-notes"
    folio_role: "backend-module"
  namespace: "reshare"
spec:
  selector:
    matchLabels:
      app: "mod-notes-5-2-0"
  replicas: 1
  template:
    metadata:
      labels:
        app: "mod-notes-5-2-0"
        module: "mod-notes"
        folio_role: "backend-module"
    spec:
      containers:
        - name: "mod-notes-5-2-0"
          image: "folioorg/mod-notes:5.2.0"
          envFrom:
            - secretRef:
                name: "folio-db-connect"
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: JAVA_OPTIONS
              value: "-XX:MaxRAMPercentage=66.0 -Dlog4j2.formatMsgNoLookups=true"
          ports:
            - containerPort: 8081
              protocol: TCP
          resources:
            limits:
              memory: 1Gi
      imagePullSecrets:
        - name: "dockerhub-cred"
